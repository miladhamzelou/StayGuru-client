/* @flow */

import asyncHome from './Home';
import asyncUserInfo from './UserInfo';
import asyncCheckIn from './CheckIn';
import NotFound from './NotFound';

export { asyncHome, asyncUserInfo, asyncCheckIn, NotFound };
